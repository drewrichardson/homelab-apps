apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: openldap-secrets
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/drewrichardson/homelab-apps.git
    targetRevision: main
    path: openldap-secrets
  destination:
    server: https://kubernetes.default.svc
    namespace: openldap
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
